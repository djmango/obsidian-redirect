name = "obsidian-redirect"
main = "worker.ts"
compatibility_date = "2024-01-01"
workers_dev = true

# Route configuration - update zone_name if needed
routes = [
  { pattern = "o.skg.gg/*", zone_name = "skg.gg" }
]
